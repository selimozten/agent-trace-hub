{
  "id": "example-generic-json-session",
  "agent": "example-agent",
  "model": "example-model",
  "tools": [
    {
      "name": "read_file",
      "description": "Read a project file.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" }
        },
        "required": ["path"]
      }
    }
  ],
  "history": [
    { "speaker": "human", "text": "Inspect the failing file." },
    {
      "speaker": "ai",
      "thinking": "The path from the issue should be opened before editing.",
      "message": "I will inspect the component.",
      "toolCalls": [
        {
          "id": "call_generic_1",
          "name": "read_file",
          "arguments": { "path": "src/App.tsx" }
        }
      ]
    },
    {
      "speaker": "tool",
      "toolCallId": "call_generic_1",
      "toolName": "read_file",
      "output": "export function App() { return null; }"
    }
  ]
}
